Prefer writing in TypeScript unless the project isn't in TypeScript.
Prefer a functional style — avoid classes.
Use const function assignments
Never omit curly braces
Prefer short, sharp variable and function names